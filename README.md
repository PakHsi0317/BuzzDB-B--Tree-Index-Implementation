# BuzzDB-B--Tree-Index-Implementation
Implemented a templated B+-Tree index structure in C++ supporting lookup, insert, erase, and range queries. Built leaf and inner node logic with binary search traversal and node splitting using page IDs through a buffer manager, enabling efficient database-style indexing.
